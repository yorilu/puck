=========================================================
压缩：
步骤一：确保build.js 里面的shim和paths配置和 main.js同步（直接copy就可以）
步骤二：在js/mini.js 里面写入你想压缩的模块
步骤三：cmd调用 node r.js -o build.js

=========================================================